{
  "name": "shocker-server",
  "version": "1.0.0",
  "description": "Node.js server for shock endpoint API",
  "main": "server.js",
  "author": "ShockerServer",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-cert": "node generate-cert.js",
    "setup": "npm run generate-cert && npm start",
    "letsencrypt": "node server.js",
    "production": "NODE_ENV=production node server.js"
  },
  "keywords": ["shocker", "api", "nodejs", "express"],
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "https": "^1.0.0",
    "fs": "^0.0.1-security",
    "node-letsencrypt": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
